<configuration-ui>
	<section name="Look">
		<tab name="Chat">
			<group-box name="Headers and separators">
				<spin-box caption="Chat header separators height" config-section="Look" config-item="HeaderSeparatorHeight" min-value="0" max-value="100" />
				<spin-box caption="Message separators height" config-section="Look" config-item="ParagraphSeparator" min-value="0" max-value="100" />
				<check-box caption="Remove chat header repetitions" config-section="Look" config-item="NoHeaderRepeat" id="noHeaderRepeat" />
				<spin-box caption="Interval between header removal" config-section="Look" config-item="NoHeaderInterval" min-value="1" max-value="1439" suffix="s" id="noHeaderInterval" />
			</group-box>

			<group-box name="Server time">
				<check-box caption="Remove server time" config-section="Look" config-item="NoServerTime" id="removeServerTime" />
				<spin-box caption="Maximum time difference" config-section="Look" config-item="NoServerTimeDiff" min-value="1" max-value="1000" suffix="s" id="maxTimeDifference" />
			</group-box>

<!-- 			<group-box name="Background"> -->
<!-- 				<line-edit caption="Full chat style" config-section="General" config-item="FullChatStyle" /> -->
<!-- 				<line-edit caption="Chat background image (experimental)" config-section="General" config-item="ChatBgImg" /> -->
<!-- 			</group-box> -->

			<group-box name="Window title">
				<line-edit caption="Chat window title syntax" config-section="Look" config-item="ChatContents"
						   tool-tip="Syntax: %s - status, %d - description, %i - ip, %n - nick, %a - altnick, %f - first name
%r - surname, %m - mobile, %u - uin, %g - group, %o - return _space_ if user doesn&apos;t have us in userlist
%v - revDNS, %p - port %e - email %x - max image size" />
				<line-edit caption="Conference window title prefix" config-section="Look" config-item="ConferencePrefix"
						   tool-tip="Syntax: %s - status, %d - description, %i - ip, %n - nick, %a - altnick, %f - first name
%r - surname, %m - mobile, %u - uin, %g - group, %o - return _space_ if user doesn&apos;t have us in userlist
%v - revDNS, %p - port %e - email %x - max image size" />
				<line-edit caption="Conference window title syntax" config-section="Look" config-item="ConferenceContents"
						   tool-tip="Syntax: %s - status, %d - description, %i - ip, %n - nick, %a - altnick, %f - first name
%r - surname, %m - mobile, %u - uin, %g - group, %o - return _space_ if user doesn&apos;t have us in userlist
%v - revDNS, %p - port %e - email %x - max image size" />
			</group-box>
		</tab>
	</section>
</configuration-ui>
