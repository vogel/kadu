set (_source_macros_file "${CMAKE_SOURCE_DIR}/cmake/KaduMacros.cmake")
if (EXISTS "${_source_macros_file}")
	# In build tree.
	include ("${_source_macros_file}")
else ()
	# In installation tree.
	include("${CMAKE_CURRENT_LIST_DIR}/KaduMacros.cmake")
endif ()
