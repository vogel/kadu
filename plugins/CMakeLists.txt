foreach (PLUGIN ${COMPILE_PLUGINS})
	message (STATUS "Plugin: " ${PLUGIN})
	if (WIN32)
		set (${PLUGIN} SHARED)
	else (WIN32)
		set (${PLUGIN} PLUGIN)
	endif (WIN32)
	add_subdirectory (${PLUGIN})
endforeach (PLUGIN)
